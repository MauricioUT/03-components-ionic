import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '03-components-ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
